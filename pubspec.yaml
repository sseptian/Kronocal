name: kronocalc
description: Kalkulator matematika + sinkronisasi 4 penanggalan (Masehi, Hijriah, Weton Jawa, Shio Cina).
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_local_notifications: ^20.1.0
  path: ^1.9.1
  sqflite: ^2.4.2
  table_calendar: ^3.2.1
  timezone: ^0.10.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true